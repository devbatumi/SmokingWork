# Path-scoped rules

Файлы здесь грузятся только когда Claude трогает соответствующие пути.
Это экономит контекст.

Примеры:
- `frontend.md` — применяется при работе с `src/**/*.tsx`
- `backend.md` — для `server/**/*.ts`
- `db.md` — при работе с миграциями / схемой
